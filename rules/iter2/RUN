for e in `cd vlp; ls *.vlp`; do
  e=${e%%.vlp}
#  echo $e
  if [ -e vlp/$e.res ]; then
    echo skipping $e
  else
    echo doing $e
    nohup inner-p --Threads=3 -y- --PrintVertices=0 --OracleItLimit=200000 --OracleTimeLimit=60 --VertexPoolSize=35 vlp/$e.vlp -o vlp/$e.res > vlp/$e.out
  fi
done

